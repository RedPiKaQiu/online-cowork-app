## 1. 管理员安全基础

- [x] 1.1 扩充 `.env.example`，添加 `ADMIN_EMAIL`、`ADMIN_PASSWORD_HASH`、`SESSION_SECRET`、`APP_URL` 及生成安全随机值的说明。
- [x] 1.2 实现密码哈希生成与恒定时间凭据验证工具，并为有效和无效凭据编写单元测试。
- [x] 1.3 实现签名、过期的管理员 Cookie 会话、登出及服务端 `requireAdmin` 保护工具，并覆盖无效和过期会话测试。
- [x] 1.4 为登录接口添加同源检查、通用错误响应与单实例短时失败限流。

## 2. 管理员登录与路由保护

- [x] 2.1 创建 `/login` 页面及登录、登出 Route Handler，登录后安全地跳转至管理员后台。
- [x] 2.2 创建服务端 `/admin` 路由保护，未认证页面请求重定向至 `/login`，已认证请求跳转至项目列表。
- [x] 2.3 为所有 `/api/admin/*` 路由接入会话与来源校验，并为未授权请求编写集成测试。

## 3. 项目管理领域与 API

- [x] 3.1 实现项目访问令牌生成、带 `PROJECT_TOKEN_PEPPER` 的哈希及一次性访问 URL 构造工具，并编写测试。
- [x] 3.2 实现管理员项目查询、创建、编辑、软删除及链接重置服务，统一处理输入验证、版本冲突和已删除项目。
- [x] 3.3 实现管理员项目 API：列表、创建、详情更新、软删除及访问链接重置；确保响应不返回令牌哈希。
- [x] 3.4 使用远程开发数据库测试项目创建、编辑、删除、旧链接失效及未授权写入拒绝。

## 4. 管理员项目后台界面

- [x] 4.1 创建 `/admin/projects` 项目列表页，展示活动项目、更新时间及新建入口。
- [x] 4.2 创建新建与编辑项目界面，支持名称/描述验证、保存状态和版本冲突提示。
- [x] 4.3 在创建和链接重置后显示一次可复制 URL，离开或刷新后不再从后台读取原始令牌。
- [x] 4.4 添加删除项目的名称确认交互，并在成功后刷新活动项目列表。
- [ ] 4.5 手工验证登录、登出、路由保护、项目生命周期和窄屏布局。

## 5. 质量与部署验证

- [ ] 5.1 验证环境变量缺失、无效 Cookie、跨站写请求和过期会话均被安全处理。
- [ ] 5.2 执行 lint、类型检查、单元/集成测试及生产构建。
- [x] 5.3 更新 README 与远程部署文档，说明管理员机密配置、生成密码哈希、HTTPS Cookie 要求和密钥轮换步骤。
