## 1. 基础配置与依赖

- [x] 1.1 确认 pnpm 可用，并添加 Drizzle、PostgreSQL 驱动、迁移工具及对应 npm 脚本。
- [x] 1.2 新增 `.env.example`，列出开发数据库连接和项目令牌 pepper 所需变量，并确认 `.env.local` 已被忽略。
- [ ] 1.3 移除 Next.js 构建中忽略 TypeScript 错误的配置，并验证类型检查与构建命令。

## 2. 数据库模型与迁移

- [x] 2.1 创建 `db/schema.ts`，定义 PostgreSQL UUID、时间戳、任务状态枚举以及 projects、members、tasks 表。
- [x] 2.2 为项目访问令牌哈希、项目软删除、成员项目归属和任务查询创建索引及约束。
- [x] 2.3 实现任务 `(assignee_id, project_id)` 到成员 `(id, project_id)` 的复合外键，验证跨项目负责人写入被拒绝。
- [x] 2.4 生成首个可审查的 SQL 迁移，并在空开发数据库上执行迁移。
- [x] 2.5 创建 `lib/db.ts`，提供服务器端复用的数据库连接且不将连接参数暴露到客户端包。

## 3. 开发数据与验证

- [x] 3.1 将现有 Demo 项目、成员和任务转换为显式运行的开发 seed 脚本。
- [x] 3.2 为 seed 加入生产环境保护，确保生产启动及迁移不会自动注入演示数据。
- [x] 3.3 验证 seed 后可按项目、状态和 position 查询完整看板数据。
- [x] 3.4 添加数据库层测试，覆盖软删除过滤、成员删除前的关联处理方案和跨项目分配拒绝。

## 4. 远程 PostgreSQL 部署文档

- [x] 4.1 编写 `docs/remote-postgres-deployment.md`：说明服务器前置条件、目录布局、环境变量与机密文件权限。
- [x] 4.2 在文档中提供远程服务器 Docker Engine 与 Compose 安装、PostgreSQL 16 Compose 文件、绑定受信任局域网 IP 并限制防火墙白名单的启动和健康检查步骤。
- [x] 4.3 在文档中提供受信任局域网连接、`DATABASE_URL` 配置、迁移和 seed 执行及常见连接故障排查步骤。
- [x] 4.4 在文档中提供 `pg_dump` 备份、恢复演练和卷持久化验证步骤。
- [x] 4.5 在文档中说明后续将 Next.js 部署到同一 Compose 网络后，切换至服务名连接并移除数据库宿主机端口发布的步骤。

## 5. 端到端验收

- [x] 5.1 使用受信任局域网连接从本地访问远程开发数据库，执行迁移、seed 和只读验证查询。
- [ ] 5.2 验证数据库端口不能从公网直连，但可从服务器本机连接。
- [ ] 5.3 重新创建 PostgreSQL 容器但保留数据卷，确认数据仍可读取。
- [ ] 5.4 执行一次备份和恢复演练，并记录恢复结果。
- [ ] 5.5 执行 lint、类型检查和生产构建，确认阶段 1 不破坏现有 Demo 页面。
