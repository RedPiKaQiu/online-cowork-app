## 1. 测试与发布门禁

- [x] 1.1 补齐 token 哈希、排序、状态迁移和成员删除取消分配的单元测试。
- [x] 1.2 补齐管理员鉴权、项目 token 隔离和跨项目 ID 越权拦截的 API 集成测试。
- [x] 1.3 建立端到端测试，覆盖登录、项目创建、匿名协作、访问链接重置及项目快照定时刷新。
- [x] 1.4 在 `package.json` 和 CI/CD（或等效受控发布脚本）中串联 `pnpm lint`、`pnpm typecheck`、`pnpm test` 与 `pnpm build`，确保任一失败均阻止生产发布。
- [ ] 1.5 为移动端基础操作、键盘操作、弹窗 Escape/焦点行为和断网提示建立可记录结果的手工验收清单。

## 2. 生产运行安全

- [x] 2.1 审核并补全 `.env.example` 与运行时环境变量校验，覆盖 `DATABASE_URL`、管理员凭据、`SESSION_SECRET`、`PROJECT_TOKEN_PEPPER` 和 `APP_URL`；确保日志和错误响应不泄露其值。
- [x] 2.2 实现或配置存活与就绪健康检查，令响应仅包含最小状态且就绪检查安全验证必要依赖。
- [x] 2.3 配置 HTTPS、正式 `APP_URL`、安全 Cookie 与受控密钥注入；验证生产环境不使用自动 seed。
- [x] 2.4 为项目删除和访问链接重置实现结构化审计事件，并添加测试证明完整 token、Cookie 和密钥不会被记录。

## 3. 可观测性与数据保护

- [x] 3.1 接入受控错误监控与基础性能指标，关联部署版本、环境和脱敏请求上下文。
- [ ] 3.2 配置连续就绪检查失败、5xx 错误率、异常响应延迟和备份任务失败的告警，并验证告警接收链路。
- [ ] 3.3 配置生产 PostgreSQL 的定期备份、保留策略和受限访问路径；在隔离目标数据库完成一次恢复演练并记录结果。

## 4. 发布与回滚自动化

- [ ] 4.1 编写受控发布流程：校验配置、创建发布前备份、执行 `pnpm db:migrate`、等待就绪后切换流量；迁移失败时保持现有版本服务。
- [x] 4.2 保存不可变构建版本及上一稳定版本，定义应用回滚、向后兼容迁移与从备份恢复的判定流程。
- [x] 4.3 实现发布后冒烟验证，使用专用验证账号和测试项目检查管理员登录、项目读取/链接访问、任务写入和项目快照定时刷新，并记录版本与结果。

## 5. 部署方案与维护指导文档

- [x] 5.1 新增部署指导文档，说明前置条件、生产环境变量、HTTPS、数据库网络限制、备份计划和监控配置。
- [x] 5.2 在文档中给出预览与生产发布步骤、质量门禁命令、迁移顺序、禁止 seed 的约束及发布后冒烟检查。
- [x] 5.3 在文档中给出告警处置、应用回滚、数据库恢复、恢复演练记录和高风险操作审计的 runbook。
- [ ] 5.4 在 README 中链接部署指导文档，并在预览环境完成一次端到端发布与回滚演练。
