## 1. 共享写入基础

- [x] 1.1 新增仅服务端可用的活动项目上下文解析器：校验并哈希链接 token、过滤软删除项目且不暴露凭据字段。
- [x] 1.2 新增共享的受限 JSON 解析、字段规范化、请求 DTO 校验及项目写入路由的安全错误响应映射。
- [x] 1.3 定义浏览器安全的写入 DTO，以及供写入服务共享的服务端成员颜色调色板与选择工具。
- [x] 1.4 为 token/项目上下文失败、输入校验和安全错误响应补充单元测试。

## 2. 项目与成员写入

- [x] 2.1 实现项目元数据写入：校验名称和描述、递增项目版本并返回安全的项目 DTO。
- [x] 2.2 新增 `PATCH /api/projects/[token]`，复用共享项目上下文和写入响应约定。
- [x] 2.3 实现项目范围内的成员创建和更新，包含服务端控制的受支持颜色及安全成员 DTO。
- [x] 2.4 以事务实现成员删除：先取消项目内关联任务的负责人，再删除成员。
- [x] 2.5 新增成员路由：`POST /api/projects/[token]/members`、`PATCH /api/projects/[token]/members/[id]` 和 `DELETE /api/projects/[token]/members/[id]`。
- [x] 2.6 测试成员校验、颜色选择、删除触发的取消分配、失效链接与跨项目成员 ID 隔离。

## 3. 任务写入与排序

- [x] 3.1 实现项目范围内的任务创建：校验标题/状态，初始不分配负责人，并将任务放到目标列末尾。
- [x] 3.2 实现任务更新和删除：校验标题/描述长度、同项目负责人、乐观版本，并强制执行移入 `box` 时取消分配的规则。
- [x] 3.3 新增 `POST /api/projects/[token]/tasks`、`PATCH /api/projects/[token]/tasks/[id]` 和 `DELETE /api/projects/[token]/tasks/[id]`，并返回安全任务数据。
- [x] 3.4 实现事务化任务重排：使用任务 ID、来源/目标状态、目标索引、预期版本和 mutation ID，连续重写受影响列的位置。
- [x] 3.5 新增 `POST /api/projects/[token]/tasks/reorder`，将陈旧版本或来源状态映射为冲突响应。
- [x] 3.6 测试任务创建、编辑、删除、同项目分配约束、`box` 状态迁移、列内/跨列重排、陈旧写入拒绝及跨项目任务隔离。

## 4. 验证与交接

- [x] 4.1 对写入服务和项目快照读取器运行定向单元测试与路由测试，确认结果持久化和项目隔离。
- [x] 4.2 运行 `pnpm lint`、`pnpm typecheck` 和 `pnpm build`，修复回归且不改变阶段 3 的读取合同。
- [x] 4.3 运行 `openspec validate persist-board-mutations --strict`，记录阶段 5 客户端接入所需的后续工作。
