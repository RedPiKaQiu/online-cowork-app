## 1. 持久化与迁移

- [x] 1.1 为 `tasks` 增加可空 `completed_at` 字段及支持已完成事项查询的索引，并生成数据库迁移。
- [x] 1.2 在迁移中以 `updated_at` 回填存量 `done` 任务的完成时间，保持非 `done` 任务为空。
- [x] 1.3 扩展任务创建、状态更新与拖拽移动逻辑，在进入、离开和重新进入 `done` 时维护 `completed_at`。

## 2. 快照与客户端展示

- [x] 2.1 调整项目快照 DTO 与查询，使 `box`、`todo` 按 `position` 升序、`done` 按 `completed_at` 降序及 `position` 升序返回。
- [x] 2.2 调整已完成抽屉的乐观更新与交互，使新完成事项即时置顶，并禁止在抽屉内拖拽重排。

## 3. 验证

- [x] 3.1 为完成时间状态转换、存量数据回填和快照排序添加测试。
- [x] 3.2 为已完成抽屉的即时置顶和禁止拖拽行为添加客户端测试。
- [x] 3.3 运行迁移、相关测试、类型检查及 `openspec validate --strict`。
