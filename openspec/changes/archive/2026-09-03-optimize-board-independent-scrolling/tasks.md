## 1. 响应式高度链

- [x] 1.1 调整 `BoardClient` 根容器、顶部栏、状态提示、看板工作区和快速添加栏的 flex/grid 约束，使桌面端中间工作区占用动态视口剩余高度且不会被任务内容撑开
- [x] 1.2 为窄屏布局保留自然页面滚动，为两个纵向任务列设置合适的最小高度和基于 `dvh` 的最大高度，避免将两列压缩为半屏区域
- [x] 1.3 调整 `QuickAddBar` 的窄屏 sticky、换行和 safe-area 布局，并确认输入框获得焦点及软键盘出现时入口仍可使用

## 2. 独立滚动区域

- [x] 2.1 调整 `TaskColumn` 外层、列标题和任务容器的尺寸与 overflow 规则，使标题固定且每列任务主体独立纵向滚动
- [x] 2.2 为桌面端任务容器增加滚动链约束和稳定 scrollbar gutter，同时保持移动端页面能够在两个任务列之间自然滚动
- [x] 2.3 为两个任务滚动区域添加与列标题关联的 accessible name、键盘焦点入口和 `focus-visible` 状态，并验证原生键盘滚动

## 3. 浏览器级回归测试

- [x] 3.1 扩展 Playwright 测试数据或辅助方法，为 `box` 和 `todo` 构造足以产生 overflow 的任务列表，并使用可访问角色稳定定位两个滚动区域
- [x] 3.2 添加桌面端测试，验证两列满足 `scrollHeight > clientHeight`、滚动位置彼此独立，并且列标题、顶部操作栏和快速添加栏保持在视口中
- [x] 3.3 添加窄屏测试，验证页面可访问两个纵向排列的受限高度任务列、列内可以滚动，并且快速添加入口保持可用
- [x] 3.4 验证长列表中的列内及跨列拖拽；若浏览器原生边缘自动滚动不可靠，补充仅作用于任务滚动容器的边缘滚动处理和相应测试

## 4. 验证

- [x] 4.1 运行相关组件测试和 `pnpm test`，确认布局与可访问性调整未影响既有看板行为
- [x] 4.2 运行新增 Playwright 场景及 `pnpm test:e2e`，覆盖桌面、低高度桌面和窄屏 viewport
- [x] 4.3 运行项目 lint/typecheck，并人工检查亮色、暗色及系统覆盖式滚动条环境下的焦点和滚动体验
