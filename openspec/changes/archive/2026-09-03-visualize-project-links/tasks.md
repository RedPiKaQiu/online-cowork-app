## 1. 加密存储与数据库

- [x] 1.1 为 `projects` 增加可空的 `access_token_ciphertext` 字段，生成数据库迁移，并验证迁移不会修改既有项目的 `access_token_hash`。
- [x] 1.2 实现基于 AES-256-GCM 和 `PROJECT_TOKEN_ENCRYPTION_KEY` 的版本化 token 加密/解密工具，覆盖正常往返、错误密钥、篡改密文及非法配置测试。
- [x] 1.3 更新环境变量示例与部署文档，说明加密密钥的生成、保管、轮换限制和丢失后的处理方式。

## 2. 管理员项目服务与 API

- [x] 2.1 更新项目创建与链接重置流程，在同一写入中保存 token 哈希和密文，并继续只用哈希验证匿名项目访问。
- [x] 2.2 扩展 `AdminProject` 与 `listActiveProjects()`，仅在服务端解密非空密文并返回 `accessUrl: string | null`，且不返回 hash、ciphertext 或原始 token 字段。
- [x] 2.3 补充管理员服务和 `GET /api/admin/projects` 测试，覆盖刷新后恢复 URL、旧项目返回 `null`、未认证访问被拒绝、重置后 URL 更新及旧 token 失效。

## 3. 管理界面链接交互

- [x] 3.1 扩展 `ProjectListItem` 和项目列表渲染，使每个具有 `accessUrl` 的项目条目显示可选择的完整 URL、复制按钮和指向对应 `/p/[token]` 的真实链接。
- [x] 3.2 增强 `ShareLink` 的持续展示文案、复制成功反馈和 Clipboard API 失败处理，确保复制失败时 URL 仍可手动选择。
- [x] 3.3 对 `accessUrl = null` 的旧项目显示链接不可用状态及进入现有设置页重置链接的入口，不自动重置或构造无效 URL。
- [x] 3.4 调整创建与重置后的客户端状态，使新 URL 立即出现在对应项目条目中，并在页面刷新后保持一致。

## 4. 验收与回归

- [x] 4.1 添加组件测试，验证每个项目的链接、复制成功/失败反馈、目标项目跳转和旧项目不可用状态。
- [x] 4.2 扩展管理员到协作页面的 E2E 测试，验证创建或重置后可复制并直接打开所选项目，且页面刷新后链接仍可用。
- [x] 4.3 运行数据库、单元、组件和 E2E 测试，并确认项目列表响应、日志与错误信息不泄露 token hash、ciphertext 或加密密钥。
