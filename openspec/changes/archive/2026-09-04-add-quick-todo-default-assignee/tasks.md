## 1. 扩展任务创建写入

- [x] 1.1 扩展 `projectBoardApi.createTask` 与创建请求 DTO，使其接受可选 `assigneeId` 并保持省略字段时的向后兼容行为
- [x] 1.2 更新 `createTaskByToken`，在 `todo` 等允许分配的状态下解析同项目负责人并随任务原子写入，在 `box` 状态下强制写入 `null`
- [x] 1.3 补充服务端 mutation 和 API 测试，覆盖同项目负责人、未分配、`box` 强制清空、无效类型及跨项目成员拒绝且不创建任务

## 2. 串联乐观创建与提交结果

- [x] 2.1 调整 `BoardClient.addTask` 和通用 mutation 结果传递，使快速添加可以等待明确的成功或失败结果
- [x] 2.2 让乐观任务使用本次提交的有效 `assigneeId`，并以独立且真实生效的快速创建 pending 状态防止重复提交
- [x] 2.3 添加 `BoardClient` 与 API 客户端测试，验证负责人请求负载、乐观负责人、成功替换以及失败后的快照恢复和结果通知

## 3. 实现快速添加默认负责人交互

- [x] 3.1 扩展 `QuickAddBar` props 和本地状态，接收成员列表、成员管理回调并维护初始为“未分配”的页面内默认负责人
- [x] 3.2 实现仅在“当前待办”目标下显示的负责人触发按钮与弹层，展示成员颜色、姓名、“未分配”选项以及无成员时的成员管理入口
- [x] 3.3 实现默认负责人连续沿用、切换到事项盒子时隐藏但保留、切回时恢复，以及成员从最新列表消失时自动重置
- [x] 3.4 调整异步提交行为：提交期间锁定当前草稿，成功后仅清空标题并恢复输入焦点，失败后保留标题和负责人供原样重试
- [x] 3.5 完成选择器的焦点、展开状态、方向键、Enter/Space、Escape 和关闭后焦点恢复等可访问交互
- [x] 3.6 调整快速栏响应式布局，使桌面端保持单行顺序、窄屏使用两行结构，并处理长成员姓名、弹层向上展开、最大高度滚动及 safe area

## 4. 验证用户流程与回归

- [x] 4.1 添加快速栏组件测试，覆盖连续添加、未分配、目标切换、成员失效、页面重建默认值、无成员入口、键盘操作及成功/失败草稿行为
- [x] 4.2 添加或扩展 Playwright 场景，验证桌面端和窄屏下负责人选择、带负责人创建、连续沿用、失败重试及快速栏无水平溢出或遮挡
- [ ] 4.3 运行 `pnpm test`、`pnpm typecheck`、`pnpm lint` 和相关 `pnpm test:e2e`，修复本次变更引入的回归
